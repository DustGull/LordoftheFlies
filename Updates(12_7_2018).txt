redundant code line 196(about) that was causing the objects
to move in positive direction, then back in negative 
direction

changed the screen size, 42, to 500,500

changed rect_list to include all of the characters/items

rearranged the characters because minimap was covering them

Changed the code that references the tile images.
you'll have to move the new images into the maps
folder.
